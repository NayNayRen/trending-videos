import { View, Text } from "react-native";
import React from "react";

const Search = () => {
	return (
		<View>
			<Text>Search</Text>
		</View>
	);
};

export default Search;

// [query] is the search screen
// using square brackets allows for dynamic routing
// NextJS uses a similar scheme
